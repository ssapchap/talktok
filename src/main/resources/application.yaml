server:
  port: 80

# JPA Setting
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/talktoktest3
    username: talktokuser
    password: 1234
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
#  data:
#    web:
#      pageable:
#        one-indexed-parameters: true

# Logging Setting
logging:
  level:
    org:
      hibernate: info

